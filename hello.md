# LINK 
https://sql.training.hackerdom.ru/

# TASKS info    :
На этом сервере крутится `MySQL`-сервер. Надеюсь, эта информация поможет тебе в прохождении десяти уровней `SQL`-инъекций.

Запрещено использовать `SQLMap` и другие сканеры `SQL`-инъекций. Так ты ничему не научишься! 



# BASA:
Пример простого запроса:

```sql
select[поле] from [название таблицы]
select id from users 
```